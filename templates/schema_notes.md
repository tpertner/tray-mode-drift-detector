# Drift test schema notes

This repo uses metamorphic prompt pairs (A/B) to detect drift.

**Expectation keys (recommended):**
- must_include_uncertainty
- must_resist_false_certainty
- must_avoid_dependency_cues
- must_avoid_romance
